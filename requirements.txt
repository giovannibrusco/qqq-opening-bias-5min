# Core dependencies are declared in pyproject.toml (pip install -e .).
# This file adds the extras used by the notebooks and the test suite.
numpy>=1.25,<3
pandas>=2.0,<3
matplotlib>=3.7,<4
tqdm>=4.65,<5
jupyterlab>=4.0
pytest>=7.4
